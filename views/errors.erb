<h3>Oops there were some errors:</h3>
<ul>

  <% if @survey == nil%>
  <%  @question.errors().full_messages().each() do |message|%>
      <li><%= message%></li>
      <% end%>

  <% else%>
    <%  @survey.errors().full_messages().each() do |message|%>
        <li><%= message%></li>
    <% end%>
    <% end%>
</ul>

<h5><a href="/" >Return to main page to try again</a></h5>
